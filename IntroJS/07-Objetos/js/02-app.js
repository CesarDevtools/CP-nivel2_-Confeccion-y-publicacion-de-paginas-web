//Otra forma de acceder a las propiedades de un objeto 

const producto = {
    nombre: "Monitor 40 \"",
    precio: 350,
    disponible: true
};

console.log(producto.nombre);
console.log(producto['precio']); // Acceso a la propiedad usando corchetes
