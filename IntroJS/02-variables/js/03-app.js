// vamos al 3er tipo de declaración de variables --const--
// --const-- es una variable que no puede cambiar su valor, es decir, es constante
// let y cont son variables de bloque, es decir, solo existen dentro del bloque donde se declaran
//let permite incializar sin valor -- // const no permite inicializar sin valor

const producto = "Monitor 20 pulgadas";
console.log(producto);

const precio = 300;

// precio = 400; // Esto generará un error porque 'precio' es una constante y no puede ser reasignada
console.log(precio);
